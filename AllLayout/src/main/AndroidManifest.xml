<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fugenx.alllayout"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.fugenx.alllayout.AllLayoutActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AbsoluteActivity" />
        <activity android:name=".FrameActivity" />
        <activity android:name=".LinearHorizontal" />
        <activity android:name=".RelativeActivity" />
        <activity android:name=".TableActivity" />
        <activity android:name=".LinearVertical"/>
        <activity android:name=".GridActivity"/>
        <activity android:name=".GridViewActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".Grid1" />
    </application>

</manifest>
